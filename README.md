# reinvent_docking

Структура проекта
```
/home/username/                          # ~/ ваша домашняя папка в WSL
│
├── 📁 miniconda3/                        # Conda (базовая)
│   ├── 📁 envs/                           # ВСЕ conda окружения
│   │   ├── 📁 reinvent4/                   # Окружение для REINVENT4
│   │   │   ├── 📁 bin/
│   │   │   │   ├── reinvent
│   │   │   │   └── python
│   │   │   └── ...
│   │   │
│   │   └── 📁 DockStream/                   # Окружение для докинга
│   │       ├── 📁 bin/
│   │       │   ├── obabel
│   │       │   ├── vina
│   │       │   ├── mk_prepare_receptor.py
│   │       │   └── python
│   │       └── ...
│   │
│   └── 📁 pkgs/                            # Кэш установленных пакетов
│
└── 📁 reinvent_docking/                             # ПАПКА ПРОЕКТА
    │
    ├── 🐍 reinvent_ppar_pipeline__wolffe.py         # ВАШ ГЛАВНЫЙ PYTHON СКРИПТ
    ├── 📄 dockstream_config.json             # Создаётся скриптом
    ├── 📄 rl_config.toml                      # Создаётся скриптом
    │
    ├── 📁 priors/                              # Папка с prior-моделями
    │   └── 📄 reinvent.prior                     # Скачанный prior файл (около 500MB)
    │
    ├── 📁 REINVENT4/                           # Клонированный репозиторий REINVENT4
    │   ├── reinvent.py
    │   ├── install.py
    │   ├── 📁 reinvent/
    │   └── ...
    │
    ├── 📁 DockStream/                          # Клонированный репозиторий DockStream
    │   ├── docker.py
    │   ├── requirements.txt
    │   ├── 📁 dockstream/
    │   └── ...
    │
    ├── 📁 targets/                             # Создаётся setup_directories()
    │   ├── ppar_alpha.pdb                      # ВАШИ PDB ФАЙЛЫ
    │   ├── ppar_gamma.pdb
    │   ├── ppar_alpha.pdbqt                    # Создаются prepare_receptors() или вручную
    │   └── ppar_gamma.pdbqt
    │
    ├── 📁 results/                             # Создаётся setup_directories()
    │   ├── rl_1.csv                              # Результаты REINVENT
    │   ├── scores_ppara.csv                      # Скоры для PPARa
    │   ├── scores_pparg.csv                      # Скоры для PPARg
    │   ├── dockstream.log                        # Лог DockStream
    │   ├── stage1.chkpt                          # Чекпоинт REINVENT
    │   └── top10_selective.csv                   # Топ-10 молекул
    │
    ├── 📁 poses/                               # Создаётся setup_directories()
    │   ├── conformers.sdf                        # Конформеры от RDKit
    │   ├── docked_ppara.sdf                       # Позы для PPARa
    │   └── docked_pparg.sdf                       # Позы для PPARg
    │
    ├── 🐚 bash_install_local.sh                           # BASH СКРИПТ УСТАНОВКИ ДЛЯ ЛОКАЛЬНОЙ МАШИНЫ
    └── 🐚 bash_install_serv.sh                            # BASH СКРИПТ УСТАНОВКИ ДЛЯ СЕРВЕРА
```
